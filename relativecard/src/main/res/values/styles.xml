<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="Base.RelativeCardView" parent="android:Widget">
        <item name="cardCornerRadius">@dimen/cardview_default_radius</item>
        <item name="cardElevation">@dimen/cardview_default_elevation</item>
        <item name="cardMaxElevation">@dimen/cardview_default_elevation</item>
        <item name="cardUseCompatPadding">false</item>
        <item name="cardPreventCornerOverlap">true</item>
    </style>
    <!-- On older platforms we do not explicitly set a background color and let CardView compute
         one based on the theme. -->
    <style name="RelativeCardView" parent="Base.RelativeCardView"/>

    <style name="RelativeCardView.Light">
        <item name="cardBackgroundColor">@color/cardview_light_background</item>
    </style>

    <style name="RelativeCardView.Dark">
        <item name="cardBackgroundColor">@color/cardview_dark_background</item>
    </style>
</resources>